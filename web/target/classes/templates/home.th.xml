<thlogic>
	<attr sel="#headId"
	      th:replace="~{fragments/head :: headFragment}"/>
	<attr sel="#play"
	      th:text="Play" th:href="@{__${T(academy.learnprogramming.util.GameMappings).PLAY}__}" />

</thlogic>